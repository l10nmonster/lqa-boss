import { defaultSystem } from '@chakra-ui/react'

// Use the default Chakra UI 3 system
// Custom professional colors will be applied directly in components
const theme = defaultSystem

export default theme 